<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>首页</title>
</head>
<body>
  <div class="menu">
    <img id="menu-img" src="img/menu.png" onclick="toggleMenu()">
    <ul>
      <% menu.map((it,index) => { %>
        <li> <a href="#module<%=index%>"><%=it.name%></a></li>
      <% }) %>
    </ul>
      <% menu.map((it,index) => { %>
        <div id="module<%=index%>" class="module"><%=it.name%></div>
      <% }) %>
  </div>
</body>
<script type="text/javascript" src="js/index.js"></script>
<style type="text/css">
  /* iPad、 PC 480px */
  @media only screen and (min-width: 768px) {
    body {
      background-color:green;
    }
    #menu-img{
      display: none;
    }
    .menu ul{
      display: block!important;
      width: 100%;
      height: 60px;
    }
    .menu ul li{
      float: left;
      padding: 20px;
    }
  }
  /* 手机 440分辨率以下（小于479px） */
  @media screen and (max-width: 768px) {
    body {
      background-color:lightblue;
    }
    #menu-img{
      width: 20px;
      height: 20px;
    }
    .menu ul {
      display: none;
    }
  }
  /* 公用 */
  .module{
    width: 300px;
    height: 500px;
  }
  .menu ul li{
    list-style: none;
  }
  .menu ul li a {
    text-decoration: none;
  }
</style>
</html>