const success = {
  code: 200,
  msg: 'Success'
}

const error = {
  code: 500,
  msg: 'Sever Error'
}

const illegal = {
  code: 403,
  msg: 'Illegal Operation'
}

const fail = {
  code: 600,
  msg: 'Operation Failed'
}

module.exports = {
  success,
  error,
  illegal,
  fail
}